#include<vector>
#include<iostream>

//This file parses through the object representations obtained from read_off, and rotates them to create several (24) instances of each object.
//It then uses voxelize.cpp to convert each instance into a voxel, represented as a 3d vector binary grid, for both the training and the 
//testing sets, to be used by train_vae.cpp

void main()
{
	return 0;
}